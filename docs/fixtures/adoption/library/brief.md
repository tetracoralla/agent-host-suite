# Finish the North Pier library icons

This is the harbor library a member sees on a first visit. The empty state, the two actions, and the sample row type marks still use dashed squares.

Please put durable icons on:

- the empty-state picture
- Upload chart and New folder
- the four sample rows (chart, tide table, photo, notice)

Keep the copy and spacing. The page should not look like a wireframe when opened. Edit `index.html` and leave icons in the file.

Do not invent a new information architecture.

---

北堤港湾资料库是成员第一次进来会看到的页。空状态、两个动作、四条示例行的类型标记还是虚线方块。

请补上能长期用的图标，并写进 `index.html`。不要改信息结构，不要只在对话里贴图。
